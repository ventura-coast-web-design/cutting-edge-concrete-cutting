---
layout: base.njk
title: "Gallery - Cutting Edge Concrete Cutting"
description: "View our portfolio of concrete cutting, sawing, and coring projects"
---

<main class="gallery-page container">
  <h1>Project Gallery</h1>
  <p class="gallery-subtitle">26 Years of Concrete Cutting, Sawing, and Coring Services Across Southern California</p>

  <div class="gallery-grid">
    {% for i in range(1, 61) %}
      {% set imageNum = ("0" + i) if i < 10 else i %}
      {% set altText = "Core drilling project" if i % 3 == 0 else ("Wall and slab sawing project" if i % 2 == 0 else "Concrete cutting project") %}
      {% image "src/assets/concrete-cutting-" + imageNum + ".webp", altText, "(min-width: 1024px) 33vw, (min-width: 768px) 50vw, 100vw" %}
    {% endfor %}
  </div>
</main>

